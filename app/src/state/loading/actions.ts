import { createAction } from '@reduxjs/toolkit'

export const updateOrder = createAction<any>('loading/updateOrder')

export const updateOptions = createAction<any>('loading/updateOptions')

export const updatePositions = createAction<any>('loading/updatePositions')
